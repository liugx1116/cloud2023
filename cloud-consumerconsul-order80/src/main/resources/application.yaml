server:
  port: 803

spring:
  application:
    name: cloud-consumer-order

  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      port: 8500
      host: localhost